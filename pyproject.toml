[project]
name = "automatic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "psutil>=7.0.0",
    "requests>=2.32.4",
    "rich>=14.0.0",
]
